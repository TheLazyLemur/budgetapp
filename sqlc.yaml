version: "2"
sql:
  - engine: "sqlite"
    queries: "./db/queries/users.sql"
    schema: "./db/migrations/"
    gen:
      go:
        package: "db"
        out: "./src/internal/db"
        emit_interface: true
        emit_json_tags: true
